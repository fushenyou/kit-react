// import KitForm from '../src/KitForm';

// describe('test KitFrom components', () => {
//     it('KitForm component should be shallow snapshots.', () => {
//         const wrapper = shallow(<KitForm config={config} />);
//         expect(toJson(wrapper)).toMatchSnapshot();
//     });

//     it('KitForm component should be mount snapshots.', () => {
//         const wrapper = mount(<KitForm config={config} />);
//         expect(toJson(wrapper)).toMatchSnapshot();
//     });

//     it('KitForm component should be render snapshots.', () => {
//         const wrapper = render(<KitForm config={config} />);
//         expect(toJson(wrapper)).toMatchSnapshot();
//     });
// });
